package org.example.backend.course.domain.entity;

/**
 * CourseType enum represents the type of course. BS - 학부 과정 (Bachelor of Science) MS - 학사 과정 (Master of Science)
 */
public enum CourseType {
    BS,
    MS
}